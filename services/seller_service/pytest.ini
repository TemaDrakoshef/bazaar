[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = -q
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    unit: unit tests that do not require infrastructure
    integration: tests that require a live database
    e2e: full end-to-end scenarios
